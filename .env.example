# PokeProxy Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Path to the JSON config file with routing rules (required)
POKEPROXY_CONFIG=config.json

# Base64-encoded HMAC secret for signature validation (required)
# Generate with: openssl rand -base64 32
POKEPROXY_SECRET=

# Maximum request body size in bytes (optional, default: 4096)
# POKEPROXY_MAX_BODY_SIZE=4096
